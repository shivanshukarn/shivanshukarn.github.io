export const metadata = {
  title: "Admin | Shivanshu Karn's Portfolio",
  description: "Shivanshu Karn's Portfolio Admin Dashboard",
};

export default function AdminLayout({ children }) {
  return (
    <main>
      {children}
    </main>
  );
}